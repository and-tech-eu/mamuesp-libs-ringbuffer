author: fms1961@gmail.com (mamuesp)
type: lib
description: An double holding ringbuffer
version: 1.0
libs_version: ${mos.version}
modules_version: ${mos.version}
mongoose_os_version: ${mos.version}

sources:
  - src

includes:
  - include 

tags:
  - c
  - docs:misc:Empty

manifest_version: 2017-09-29
